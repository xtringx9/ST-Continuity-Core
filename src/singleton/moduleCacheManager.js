import { getContext, extension_settings, saveSettingsDebounced, infoLog, errorLog, debugLog } from "../index.js";
class ModuleCacheManager {
    constructor() {
    }
}

// 创建单例实例
const moduleCacheManager = new ModuleCacheManager();
export default moduleCacheManager;
